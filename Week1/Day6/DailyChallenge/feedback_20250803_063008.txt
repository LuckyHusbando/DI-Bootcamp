URL: https://github.com/LuckyHusbando/DI-Bootcamp/blob/main/Week1/Day6/DailyChallenge/DC-Dictionaries.py
suggestions for improvement:
- In Challenge 1, consider adding error handling for non-string inputs.  The current code will crash if the user enters something that's not a word.
- In Challenge 2, the `cleaned_prices` dictionary creation could be slightly more efficient by using a list comprehension, potentially improving readability and minor performance gains.  Also, consider adding error handling for invalid price formats in the input dictionary.
- For both challenges, adding docstrings to explain the purpose and parameters of functions would improve readability.
Brief justification:
- correctness: Both challenges are correctly implemented. Challenge 1 accurately creates a dictionary mapping each character in a word to a list of its indices, fulfilling all requirements.  Challenge 2 correctly cleans the input data, calculates affordable items, sorts them, and handles the case where nothing is affordable.  The code directly addresses all aspects detailed in the Chapter Content/Questions, using dictionaries, loops, conditionals, and string manipulation as specified.
- readability: The code is well-structured and relatively easy to understand. The variable names are descriptive.  However, the lack of docstrings and some minor improvements in code style (e.g., using list comprehension for better efficiency in Challenge 2) could slightly improve readability.
- performance: The performance is good for the tasks. The time complexity for Challenge 1 is linear O(n), where n is the length of the word. The time complexity for Challenge 2 is also linear, primarily determined by the iteration through the `items_purchase` dictionary.  However, minor optimizations are possible (as suggested in the feedback) to reduce the number of operations and improve performance.
- security: The code is secure as it does not have any obvious vulnerabilities. It correctly handles user input and the provided data without any potential risks such as buffer overflows or cross-site scripting (XSS) attacks. The code does not interact with external resources, which eliminates many common security concerns.

